'use client'

import { Children } from "react"

const CalculationButton = () => {
    
    return(
        <>
        <button onClick={Children} type="Submit">Calculate</button>
        </>
    )
}

export default CalculationButton;